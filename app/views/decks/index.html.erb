<h1>Decks#index</h1>
<p>Find me in app/views/decks/index.html.erb</p>
<%= render 'search' %>


<ul>
  <% @decks.each do |deck| %>
    <li><%= link_to deck.name, deck_path(deck) %></li>
  <% end %>
</ul>
